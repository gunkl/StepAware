# Device-specific secrets for StepAware tooling.
# This file is a template. Copy it to secrets.env and fill in real values.
# secrets.env is gitignored and must never be committed.

# IP address of your ESP32 device on the local network.
# Used by skills like /coredump and any curl-based device interactions.
DEVICE_IP=your.device.ip.here
